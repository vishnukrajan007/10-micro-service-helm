name: AdService CI

on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - 'src/adservice/**'

jobs:
  call-workflow:
    uses: ./.github/workflows/reusable-build.yml
    with:
      service_name: adservice
    secrets: inherit
