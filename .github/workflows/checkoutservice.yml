name: CheckoutService CI

on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - 'src/checkoutservice/**'

jobs:
  call-workflow:
    uses: ./.github/workflows/reusable-build.yml
    with:
      service_name: checkoutservice
    secrets: inherit
