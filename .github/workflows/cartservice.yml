name: CartService CI

on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - 'src/cartservice/**'

jobs:
  call-workflow:
    uses: ./.github/workflows/reusable-build.yml
    with:
      service_name: cartservice
    secrets: inherit
